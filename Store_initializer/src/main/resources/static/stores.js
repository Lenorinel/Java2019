function sendStore(csrf, storeId, name, address, comment, workTime) {
    let body = {
        "name": name,
        "address": address,
        "comment": comment,
        "worktime": workTime
    };


}
